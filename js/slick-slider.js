$(document).ready(function(){
  $('.big_visual').slick({
    infinite: true,
    speed: 300,
    resizing: true,
    slidesToShow: 1,
    autoplay: true,
    autoplaySpeed: 6000,
    fade: true
  });
});